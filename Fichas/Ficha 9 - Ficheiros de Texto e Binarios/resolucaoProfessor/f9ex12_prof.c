#include <stdio.h>

long lerNumeros(int *num){
    long n = 0;
    int *ptr = num;

    FILE *fp = fopen(PATH, "rb");
    if (fp == NULL){
        printf("Erro ao abrir '%s' para escrita\n", PATH);
        prtinf("%d %d 
    }
}

int main(){

    int op;
    while(1){
        switch(op){
            case 1:
            break;

            case 2:
            break;

            case 3:
            break;

            case 4:
        }
    }
}